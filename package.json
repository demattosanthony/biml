{
  "name": "biml",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "bimlc": "bun packages/biml/bin/cli.ts",
    "test": "bun run test:biml && bun run test:compiler",
    "test:biml": "cd packages/biml && bun test",
    "test:compiler": "cd packages/compiler && uv sync --extra dev && uv run pytest",
    "build": "cd packages/biml && bun run build",
    "build:viewer": "cd packages/ifc-viewer && bun run build"
  }
}
